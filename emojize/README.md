### This program promts the user to input a string then returns an emoji that corresponds with that string if the string is valid.

# Example
string = ":thumbs_up:"
print("Output: ", emoji.emojize(switch, language = 'alias'))
>> 👍